# mymodule.py — version 1 (pre-reload)

def compute(n):
    """v1: doubles the input."""
    return n * 2

def transform(value, n):
    """v1: adds n to value."""
    return value + n

def aggregate(history):
    """v1: sums the history list."""
    return sum(history)
